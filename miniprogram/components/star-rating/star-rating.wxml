<view class="star-rating">
  <view 
    class="star {{index < value ? 'active' : ''}} {{readonly ? 'readonly' : ''}}"
    wx:for="{{[1,2,3,4,5]}}"
    wx:key="index"
    data-value="{{index + 1}}"
    bindtap="handleTap"
    style="font-size: {{size}}rpx;"
  >★</view>
</view>
