# MongoDB 连接串（Docker 部署默认即可，云部署改为 MongoDB Atlas URI）
MONGODB_URI=mongodb://localhost:27017/better-coffee

# 后端端口
PORT=3000

# CORS 允许来源（生产改为你的域名）
CORS_ORIGIN=*

# Demo 模式：未配置 API Key 时返回模拟 AI 推荐 + 启动时播种示例数据
DEMO_MODE=true
